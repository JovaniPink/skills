@AGENTS.md

Claude-specific projection: canonical skills live in `skills/`; the Claude plugin tree is generated. Explicit-only skills use `disable-model-invocation: true` only in generated Claude frontmatter.
