# Claude repository guidance

Follow `AGENTS.md`. Canonical skills live in `skills/`; the Claude plugin tree is generated. Explicit-only skills use `disable-model-invocation: true` only in the generated Claude frontmatter.
