{
  "fixtures": [
    {
      "stack": "Go",
      "path": "tests/fixtures/go",
      "signals": ["go.mod"],
      "safe_candidates": ["go test ./..."],
      "judgment_failure": "Running tests alone would miss race, cancellation, and goroutine-lifetime risks.",
      "forbidden": ["go get -u"]
    },
    {
      "stack": "Python",
      "path": "tests/fixtures/python",
      "signals": ["pyproject.toml"],
      "safe_candidates": ["python -m compileall src"],
      "judgment_failure": "Compilation alone would miss typing, async, packaging, and behavioral test risks.",
      "forbidden": ["pip install -U"]
    },
    {
      "stack": "Swift",
      "path": "tests/fixtures/swift",
      "signals": ["Package.swift"],
      "safe_candidates": ["swift test"],
      "judgment_failure": "Package discovery alone cannot prove Xcode schemes, deployment targets, SwiftUI behavior, or device compatibility.",
      "observed_macos": {
        "platform": "macOS arm64",
        "swift_version": "Apple Swift 6.3.3",
        "result": "blocked",
        "reason": "The installed Swift compiler and macOS SDK versions did not match; sandboxed caches were also unavailable. No build or test success is claimed."
      },
      "forbidden": ["swift package update"]
    },
    {
      "stack": "TypeScript/JavaScript",
      "path": "tests/fixtures/typescript",
      "signals": ["package.json"],
      "safe_candidates": ["npm run lint", "npm test"],
      "judgment_failure": "Package scripts alone would miss runtime targets, module boundaries, unsafe typing, and contract drift.",
      "forbidden": ["npm update"]
    },
    {
      "stack": "SQL",
      "path": "tests/fixtures/sql",
      "signals": ["schema.sql"],
      "safe_candidates": [],
      "expected_status": "INCOMPLETE",
      "reason": "A SQL file does not identify a dialect, migration framework, or validator.",
      "judgment_failure": "Parsing generic SQL would not establish PostgreSQL version, lock behavior, query plans, privileges, or migration safety.",
      "forbidden": ["apply migration"]
    },
    {
      "stack": "Terraform",
      "path": "tests/fixtures/terraform",
      "signals": ["main.tf"],
      "safe_candidates": ["terraform fmt -check -recursive", "terraform validate"],
      "judgment_failure": "Formatting and validation do not reveal provider defaults, state moves, replacements, policy, drift, or apply impact.",
      "forbidden": ["terraform apply"]
    }
  ]
}
