interface:
  display_name: "Finish Development Branch"
  short_description: "Choose safe integration or cleanup boundaries"
  default_prompt: "Use $finish-development-branch to reconcile this branch and present safe next-state options."
policy:
  allow_implicit_invocation: false
