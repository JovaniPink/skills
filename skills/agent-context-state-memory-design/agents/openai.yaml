interface:
  display_name: "Agent Context State Memory Design"
  short_description: "Design safe agent information lifecycles"
  default_prompt: "Use $agent-context-state-memory-design to map context, state, memory, identity, and retention boundaries."
policy:
  allow_implicit_invocation: true
