interface:
  display_name: "Public-Private Boundary Review"
  short_description: "Check artifacts before public exposure"
  default_prompt: "Use $public-private-boundary-review to audit this artifact before it becomes public."
policy:
  allow_implicit_invocation: true
