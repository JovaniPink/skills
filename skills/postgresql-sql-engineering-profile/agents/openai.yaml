interface:
  display_name: "PostgreSQL and SQL Engineering Profile"
  short_description: "Apply focused PostgreSQL and SQL guidance"
  default_prompt: "Use $postgresql-sql-engineering-profile to apply focused stack guidance after repository discovery."
policy:
  allow_implicit_invocation: true

