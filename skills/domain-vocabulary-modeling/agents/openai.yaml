interface:
  display_name: "Domain Vocabulary Modeling"
  short_description: "Build a traceable proposed domain vocabulary"
  default_prompt: "Use $domain-vocabulary-modeling to model the domain language and preserve definition conflicts."
policy:
  allow_implicit_invocation: true
