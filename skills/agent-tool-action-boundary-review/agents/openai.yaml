interface:
  display_name: "Agent Tool Action Boundary Review"
  short_description: "Review agent tools and external effects"
  default_prompt: "Use $agent-tool-action-boundary-review to review this agent's registered tools, identity, policy, and effects."
policy:
  allow_implicit_invocation: true
