interface:
  display_name: "Systematic Diagnosis"
  short_description: "Find causes before proposing fixes"
  default_prompt: "Use $systematic-diagnosis to reproduce and isolate the cause of this failure."
policy:
  allow_implicit_invocation: true
