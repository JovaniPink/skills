interface:
  display_name: "Portable Skill Authoring"
  short_description: "Build portable skills with safe client adapters"
  default_prompt: "Use $portable-skill-authoring to design this capability as a portable, evaluated Agent Skill."
policy:
  allow_implicit_invocation: true
