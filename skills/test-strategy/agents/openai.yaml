interface:
  display_name: "Test Strategy"
  short_description: "Design risk-proportionate test coverage"
  default_prompt: "Use $test-strategy to map this change's risks to appropriate test layers."
policy:
  allow_implicit_invocation: true
