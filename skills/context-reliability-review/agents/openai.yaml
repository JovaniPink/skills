interface:
  display_name: "Context Reliability Review"
  short_description: "Audit AI context assertions and lifecycle"
  default_prompt: "Use $context-reliability-review to review the provenance, timing, permissions, and lifecycle of this context."
policy:
  allow_implicit_invocation: true
