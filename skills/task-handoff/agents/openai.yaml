interface:
  display_name: "Task Handoff"
  short_description: "Transfer exact state without transferring authority"
  default_prompt: "Use $task-handoff to prepare a verified continuation record for this task."
policy:
  allow_implicit_invocation: false
