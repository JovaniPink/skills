interface:
  display_name: "Agent Protocol Interoperability Review"
  short_description: "Review agent protocol trust and compatibility"
  default_prompt: "Use $jovanipink-agent-platforms:agent-protocol-interoperability-review to review this A2A, MCP, or agent protocol boundary."
policy:
  allow_implicit_invocation: true
