interface:
  display_name: "Code Change Review"
  short_description: "Find verified defects in an exact code change"
  default_prompt: "Use $jovanipink-engineering:code-change-review to review this exact change and report only evidence-supported findings."
policy:
  allow_implicit_invocation: true
