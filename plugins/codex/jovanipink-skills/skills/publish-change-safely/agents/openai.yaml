interface:
  display_name: "Publish Change Safely"
  short_description: "Publish only authorized repository changes"
  default_prompt: "Use $jovanipink-skills:publish-change-safely to publish this scoped change and stop at the authorized boundary."
policy:
  allow_implicit_invocation: false
