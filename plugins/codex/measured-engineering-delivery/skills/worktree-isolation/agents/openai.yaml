interface:
  display_name: "Worktree Isolation"
  short_description: "Assess safe isolation for concurrent Git work"
  default_prompt: "Use $measured-engineering-delivery:worktree-isolation to assess whether this work needs a linked worktree."
policy:
  allow_implicit_invocation: true
